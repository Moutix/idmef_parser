---
name: AlternativeID
description: 'The AlternativeID class lists the incident tracking numbers used by
  CSIRTs, other than the one generating the document, to refer to the identical activity
  described the IODEF document.  A tracking number listed as an AlternativeID references
  the same incident detected by '
color: "#99CC00"
aggregates:
  IncidentID:
    name: IncidentID
    multiplicity: 1..*
    type: 
    description: The incident tracking number of another CSIRT.
attributes:
  restriction:
    name: restriction
    multiplicity: Optional
    type: ENUM
    description: This attribute has been defined in Section 3.2.
